{
  "providers": {
    "python": {
      "installCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput",
      "startCommand": "gunicorn real_estate_portal.wsgi --workers 3 --bind 0.0.0.0:$PORT"
    }
  },
  "disableNode": true
}